
let str=('abcdef');
//Returns true if the sequence of elements of searchStringo
//  is the same as the corresponding elements 
//  at starting position. Otherwise returns false.

console.log(str.startsWith('ab'));//true
console.log(str.startsWith('ab',1));//its searching from 1 index n 1 index 
// present 'b' not 'a' so give false

